{
  "title": "iCalendar API",
  "config": {
    "abort": {
      "already_configured": "This calendar selection is already configured.",
      "reconfigure_successful": "Reconfiguration successful."
    },
    "error": {
      "invalid_secret": "Secret must be at least 20 characters and contain only ASCII letters, digits, underscores, or hyphens.",
      "entity_not_found": "Selected calendar entity is unavailable. Ensure it is loaded and try again.",
      "no_calendars": "Select at least one calendar to include.",
      "invalid_selection_mode": "Choose include or exclude.",
      "invalid_geocoding_url": "Enter an HTTP(S) Nominatim search endpoint without credentials, query parameters, or a fragment."
    },
    "step": {
      "user": {
        "title": "Add iCalendar feed",
        "description": "Create one secure iCalendar URL for multiple calendars. Include exports only the selected calendars. Exclude exports all calendars except those selected; leave the list empty to export all calendars.",
        "data": {
          "selection_mode": "Selection mode",
          "calendar_entity_ids": "Calendars",
          "geocoding_url": "Address lookup URL (optional)"
        },
        "data_description": {
          "geocoding_url": "Nominatim-compatible search endpoint. Sends event locations to this provider for Apple Calendar maps. Leave empty to disable. See the documentation for provider requirements."
        }
      },
      "reconfigure": {
        "title": "Reconfigure iCalendar feed",
        "description": "{url_block}\n\nLeave secret key empty to keep the existing one. Include exports only the selected calendars. Exclude exports all calendars except those selected; leave the list empty to export all calendars.",
        "data": {
          "secret": "New secret key (optional; minimum 20 chars)",
          "selection_mode": "Selection mode",
          "calendar_entity_ids": "Calendars",
          "geocoding_url": "Address lookup URL (optional)"
        },
        "data_description": {
          "geocoding_url": "Nominatim-compatible search endpoint. Sends event locations to this provider for Apple Calendar maps. Leave empty to disable. See the documentation for provider requirements."
        }
      }
    }
  },
  "options": {
    "error": {
      "invalid_secret": "Secret must be at least 20 characters and contain only ASCII letters, digits, underscores, or hyphens.",
      "entity_not_found": "Selected calendar entity is unavailable. Ensure it is loaded and try again.",
      "no_calendars": "Select at least one calendar to include.",
      "invalid_selection_mode": "Choose include or exclude.",
      "already_configured": "This calendar selection is already configured.",
      "invalid_geocoding_url": "Enter an HTTP(S) Nominatim search endpoint without credentials, query parameters, or a fragment."
    },
    "step": {
      "init": {
        "title": "iCalendar feed settings",
        "description": "{url_block}\n\nLeave secret key empty to keep the existing one. Include exports only the selected calendars. Exclude exports all calendars except those selected; leave the list empty to export all calendars.",
        "data": {
          "secret": "New secret key (optional; minimum 20 chars)",
          "selection_mode": "Selection mode",
          "calendar_entity_ids": "Calendars",
          "geocoding_url": "Address lookup URL (optional)"
        },
        "data_description": {
          "geocoding_url": "Nominatim-compatible search endpoint. Sends event locations to this provider for Apple Calendar maps. Leave empty to disable. See the documentation for provider requirements."
        }
      }
    }
  },
  "selector": {
    "selection_mode": {
      "options": {
        "include": "Include selected calendars",
        "exclude": "Exclude selected calendars"
      }
    }
  }
}
